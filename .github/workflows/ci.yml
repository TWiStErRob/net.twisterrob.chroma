name: "CI"

on:
  push:

concurrency:
  group: ${{ github.ref == 'refs/heads/master' && format('ci-master-{0}', github.sha) || format('ci-{0}', github.ref) }}
  cancel-in-progress: true

jobs:

  validate:
    name: "🦺 Validation"
    uses: TWiStErRob/github-workflows/.github/workflows/validate.yml@104620e2c495f0c905fde6f15ea2b6453ae4eaed # v3
    permissions:
      contents: read
      security-events: write
      actions: read


  build:
    name: "🔨 Build"
    timeout-minutes: 20
    needs:
      - validate

    permissions:
      contents: read

    runs-on: ubuntu-24.04
    steps:
      - name: "Checkout ${{ github.ref }} in ${{ github.repository }} repository."
        uses: actions/checkout@v7

      - name: "Set up Java."
        uses: actions/setup-java@v5
        with:
          java-version-file: .java-version
          distribution: temurin

      - name: "Set up Gradle."
        uses: gradle/actions/setup-gradle@v6

      - name: "Build project."
        shell: bash
        run: >
          ./gradlew
          --stacktrace
          --no-daemon
          build
